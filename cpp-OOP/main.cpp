#include "utilities.h"
#include "terrain.h"

int main() {
	
	//getName();
	//getAge();

	Terrain terrain(10, 10);
	terrain.printInfos();

	Terrain terrain2(5, 10);
	terrain2.printInfos();
	return 0;
}